openapi: "3.0.3"
info:
  title: Malformed YAML
  description: This file contains intentional YAML syntax errors.
    The indentation below is wrong and the mapping key is unclosed.
  version: "1.0.0"
paths:
  /broken:
    get:
      summary: Broken operation
        description: Extra indentation makes this a syntax error.
      responses:
        "200":
          description: [unclosed bracket
          content:
            application/json:
              schema:
                type: object
                properties:
                  key: value: bad: nested: colons
